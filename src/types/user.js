/**
 * @typedef {Object} User
 * @property {string} id - formato: "{name-slug}-{hash8}"
 * @property {string} name - nombre original del usuario
 * @property {number} createdAt - timestamp Unix (ms)
 */

export {}
